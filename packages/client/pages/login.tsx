import type { NextPage } from "next";
import Head from "next/head";

const Login: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Login</title>
      </Head>
      Login page
    </div>
  );
};

export default Login;
